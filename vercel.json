{
  "build": {
    "env": {
      "NODE_VERSION": "20",
      "PNPM_VERSION": "9"
    }
  },
  "buildCommand": "pnpm install:dev && pnpm build",
  "github": {
    "enabled": true,
    "silent": false,
    "autoJobCancelation": true,
    "allowedBranches": ["main", "staging", "development"]
  }
}
